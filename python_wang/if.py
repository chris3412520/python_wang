"""这里的 2 是一个整数常量，用作条件表达式。Python 中的条件表达式会根据其“真值”（truthiness）来决定 if 语句的分支执行情况。具体来说：
	•	非零数值（如 1, -1, 2 等）在布尔上下文中被视为 True。
	•	零（如 0）在布尔上下文中被视为 False。
	•	空容器（如 [], {}, '' 等）也被视为 False。
	•	非空容器 被视为 True。"""
if 2:
    print(5)
else:
    print(6)